#include <stdio.h>

int main() {
    double num1, num2;
    
    scanf("%lf", &num1);
    

    scanf("%lf", &num2);
    
    // Checking if num1 is equal to num2
    if (num1&&num2) {
        printf("True");
    } else {
        printf("False");
    }
    
    return 0;
}
